[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts =
    -v
    --basetemp=/tmp/pytest
    --tb=short
filterwarnings =
    once
    ignore
    default::Warning:ere\..*
asyncio_mode = auto
markers =
    integration: requires a running FerretDB/MongoDB instance
