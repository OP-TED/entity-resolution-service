[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts =
    -v
    --basetemp=/tmp/pytest
    --tb=short
    --strict-markers
log_format = %(asctime)s %(levelname)-8s %(name)s %(message)s
log_date_format = %Y-%m-%dT%H:%M:%S
filterwarnings =
    once
    ignore
    default::Warning:ere\..*
asyncio_mode = auto
markers =
    unit: fast, no I/O, domain/service/adapter layer tests
    feature: BDD feature tests (pytest-bdd step definitions)
    e2e: end-to-end tests against a near-real stack
    integration: requires a running FerretDB/MongoDB instance

;TODO: temporary exclusion of ERS_API tests, as they are currently broken due to the API changes in FerretDB
    integration_ers_api: requires a running FerretDB instance with ERS API enabled
norecursedirs = tests/feature/link_curation_api